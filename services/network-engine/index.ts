// Network Effects Engine scaffold
export function recordReferral(referrerId: string, refereeId: string) {
  // persist referral and compute rewards
  console.log('recordReferral', { referrerId, refereeId });
}

export default { recordReferral };
